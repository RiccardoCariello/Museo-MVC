@using Museo_MVC.Models
@model Souvenir;

<h1>Aggiungi qui il tuo Souvenir</h1>
<div class="container">
	<form asp-controller="Home" asp-action="Create" method="post">
		<div class="row">
			<div class="col">

				@Html.AntiForgeryToken()
				<div class="mb-3">
					<label asp-for="Img" class="form-label">Immagine</label>
					<input asp-for="Img" type="text" class="form-control" placeholder="URL/Percorso..." required>
					<span asp-validation-for="Img" class="text-danger"></span>
				</div>
				<div class="mb-3">
					<label asp-for="Name" class="form-label">Nome</label>
					<textarea asp-for="Name" rows="10" class="form-control" placeholder="Nome..."></textarea>
					<span asp-validation-for="Name" class="text-danger"></span>
				</div>
				<div class="mb-3">
					<label asp-for="Description" class="form-label">Descrizione</label>
					<input asp-for="Description" type="text" class="form-control" placeholder="Descrizione..." required>
					<span asp-validation-for="Description" class="text-danger"></span>
				</div>
				<div class="mb-3">
					<label asp-for="Price" class="form-label">Prezzo</label>
					<input asp-for="Price" type="text" class="form-control" placeholder="Prezzo..." required>
					<span asp-validation-for="Price" class="text-danger"></span>
				</div>
				<div class="mb-3">
					<p>Categoria:</p>
					<select asp-for="CategoryId" title="SouvenirCategory" rows="10" class="d-block w-100">
						<option value=""></option>
						@foreach (Category category in Model.Categories ?? new List<Category>())
						{
							<option value="@category.Id" selected="@(Model.Souvenirs.CategoryId == category.Id ? "selected" : null)">@category.Name</option>
						}
					</select>
				</div>
			</div>
		</div>
		<div class="row">
			<div class="col">
				<button type="submit" class="btn btn-primary">Aggiungi</button>
			</div>
		</div>
	</form>
</div>